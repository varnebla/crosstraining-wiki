---
import Layout from '@layouts/Layout.astro';

interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
---

<Layout title={`${title} | Crosstraining Wiki`} description={description}>
	<slot />
</Layout>
